--- CMakeLists.txt.orig	2024-04-20 13:42:23 UTC
+++ CMakeLists.txt
@@ -314,7 +314,7 @@ endif()
     set(MACOSX_BUNDLE_BUNDLE_VERSION "1.0.0")
 endif()
 
-if(NOT ${CMAKE_SYSTEM_NAME} MATCHES "Linux")
+if(NOT ${CMAKE_SYSTEM_NAME} MATCHES "FreeBSD")
     add_subdirectory("third_party/sdl2")
 else()
     find_package(SDL2)
