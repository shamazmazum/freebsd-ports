--- CMakeLists.txt.orig	2020-04-21 06:58:33.297851000 +0300
+++ CMakeLists.txt	2020-04-21 06:58:37.978087000 +0300
@@ -5,6 +5,9 @@
 include(FindPkgConfig)
 include(GNUInstallDirs)
 
+if(${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
+  set(CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH} "${CMAKE_SOURCE_DIR}/Modules")
+endif(${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
 
 ### Helpers
 
@@ -208,17 +211,22 @@
 endif(WIN32)
 
 # macOS has problems with using pkgconfig to find SDL2
-if(APPLE)
+if(APPLE OR ${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
     find_package(sdl2 REQUIRED)
-else(APPLE)
+else(APPLE OR ${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
     pkg_check_modules(SDL2 sdl2 REQUIRED)
-endif(APPLE)
+endif(APPLE OR ${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
 
 find_package(PNG REQUIRED)
 find_package(JPEG REQUIRED)
 
-pkg_check_modules(VORBISFILE vorbisfile REQUIRED)
-pkg_check_modules(JSONCPP jsoncpp REQUIRED)
+if(${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
+  find_package(JsonCPP REQUIRED)
+  find_package(VorbisFile REQUIRED)
+else(${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
+  pkg_check_modules(VORBISFILE vorbisfile REQUIRED)
+  pkg_check_modules(JSONCPP jsoncpp REQUIRED)
+endif(${CMAKE_SYSTEM_NAME} STREQUAL "FreeBSD")
 
 include_directories(
     ${OPENAL_INCLUDE_DIR}
